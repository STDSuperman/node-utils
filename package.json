{
  "name": "repo-do-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "cs": "pnpm changeset",
    "dev": "pnpm -r --parallel dev",
    "release": "pnpm changeset version && pnpm install && pnpm publish -r --access public",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
